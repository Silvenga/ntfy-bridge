services:
  ntfy-bridge:
    build: .
    ports:
      - "127.0.0.1:8080:8080"
    env_file:
      - .env
    develop:
      watch:
        - action: rebuild
          path: src
        - action: rebuild
          path: Cargo.toml
        - action: rebuild
          path: Cargo.lock
